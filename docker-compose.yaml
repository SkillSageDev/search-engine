services:
  backend:
    build: ./backend
    ports:
      - 8000:8000

  frontend:
    image: nginx:alpine
    ports:
      - 8080:80
    volumes:
      - ./frontend/:/usr/share/nginx/html/
    depends_on:
      backend
